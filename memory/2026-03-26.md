# 2026-03-26

## Task: Andrew's review feedback on AI Trainer + AI Classifier docs

Andrew reviewed PR #55 and provided specific corrections:

### Trainer fixes applied:
1. Section "How Training Works" — step 4 (Export) was wrong:
   - Was: "serialize the trained model to the configured `.joblib` file path"
   - Fixed: "save the trained model to the configured path in AI Storage"
2. Step 5 (Register) removed as unnecessary — models auto-exist in AI Storage after training
3. Text graphic replaced with mermaid diagram showing AI Storage relationship
4. All .joblib references removed from Purpose, Prerequisites, Behavior, Example sections

### Classifier fixes applied:
1. Prerequisite tip corrected — no .joblib file required; references AI Storage
2. "Path to load trained model from" field renamed conceptually to "Path in AI Storage"
3. Path field now documents `:version` and `:latest` suffixes
4. All .joblib references removed from Prerequisite, Behavior, Example sections

### Key facts learned from Andrew:
- Models are stored in cluster's AI Storage (specialized controller)
- Path entered in Trainer (e.g., `a/b/c/mymodel`) creates a model at that path in AI Storage
- Each training run creates a new version with a running number
- Classifier references: `path` or `path:version` or `path:latest`
- Currently no export from AI Storage — future feature
- Step 5 in original Trainer doc was completely wrong

### PR: #55 (doc-engineer/ai-classifier) — updated and pushed
