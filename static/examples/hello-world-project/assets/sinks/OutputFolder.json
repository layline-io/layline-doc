{
  "id": "output-folder",
  "name": "OutputFolder",
  "description": "Writes processed files to the output folder",
  "type": "FileSink",
  "config": {
    "directory": "./output",
    "createDirectory": true,
    "fileName": "greetings-${timestamp}.csv"
  }
}
