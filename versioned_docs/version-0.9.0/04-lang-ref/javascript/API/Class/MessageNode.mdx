---
custom_edit_url: null
---

# MessageNode
<div className="doc-side">
<div className="doc-main-source">

[source](https://github.com/layline-io/layline-docusaurus/blob/42f6cb9/projects/layline/src/Class/MessageNode.ts#L16)

</div>
</div>

<div className="doc-subtitle">

Individual node of a [Message](Message)

Assume that we have a data dictionary structure of
* CSV
     * RECORD_TYPE ...
     * FIRST_NAME ...
     * LAST_NAME ...
     * ...

Then `message.data.CSV` is a MessageNode as is `message.data.CSV.RECORD_TYPE`.
As an example of use check [Message.findStatus](Message#findStatus).

</div>

```ts
class MessageNode
```

## Constructor

```ts
new MessageNode(): MessageNode
```